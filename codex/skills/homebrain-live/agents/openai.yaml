interface:
  display_name: "HomeBrain Live"
  short_description: "Inspect and operate named HomeBrain instances."
  default_prompt: "Use $homebrain-live to select the requested HomeBrain target, inspect it, deploy changes, and verify the live result."

policy:
  allow_implicit_invocation: true
