#pragma once

// Replace these placeholders with your local network and panel registration values.
// The tracked file intentionally contains no secrets so it can stay in git.

#ifndef HOMEBRAIN_PANEL_WIFI_SSID
#define HOMEBRAIN_PANEL_WIFI_SSID "YOUR_WIFI_SSID"
#endif

#ifndef HOMEBRAIN_PANEL_WIFI_PASSWORD
#define HOMEBRAIN_PANEL_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#endif

#ifndef HOMEBRAIN_PANEL_HUB_URL
#define HOMEBRAIN_PANEL_HUB_URL "http://homebrain.local:3000"
#endif

#ifndef HOMEBRAIN_PANEL_ID
#define HOMEBRAIN_PANEL_ID "replace-with-panel-id"
#endif

#ifndef HOMEBRAIN_PANEL_REGISTRATION_CODE
#define HOMEBRAIN_PANEL_REGISTRATION_CODE "HBWP-XXXX-XXXX-XXXX"
#endif

#ifndef HOMEBRAIN_PANEL_FIRMWARE_VERSION
#define HOMEBRAIN_PANEL_FIRMWARE_VERSION "0.1.0-dev"
#endif

#ifndef HOMEBRAIN_PANEL_HOSTNAME
#define HOMEBRAIN_PANEL_HOSTNAME "homebrain-bedroom-panel"
#endif
